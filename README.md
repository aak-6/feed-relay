# feed-relay
Small stdlib-only scripts that poll public RSS/JSON sources and relay matches to webhooks on a schedule.
All targets and endpoints are supplied at runtime from an encrypted secret; nothing in this repo is user-specific.
